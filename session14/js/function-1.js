// we used function to reused the process in it
//  function getAvg(x, y) {
//   /*x,y --> parametars */
//   var sum = x + y;
//   var result = sum / 2;
//   console.log(result);
// }
// getAvg(10, 20); //  Invoke or call function /*10,20 --> argumants*/

// function getAvg(x, y) {
//   var sum = x + y; // 1020
//   var result = sum / 2; // 510
//   console.log(result);
// }
// getAvg(10, '20');
function getAvg(x, y) {
  // y --> undefined
  var sum = x + y; // undefined
  console.log(result);
  var result = sum / 2; // NaN
  console.log(result);
}
getAvg(10);
